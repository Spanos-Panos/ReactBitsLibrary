Install
CLI
pnpm = pnpm add styled-components
npm = npm install styled-components
yarn = yarn add styled-components
bun = bun add styled-components

Install
MANUAL
pnpm = pnpm add styled-components
npm = npm install styled-components
yarn = yarn add styled-components
bun = bun add styled-components

Source: UIverse / community logout button pattern (adapted for BitForge).
